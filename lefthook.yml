pre-commit:
  commands:
    staged:
      run: pnpm exec vp staged

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

prepare-commit-msg:
  commands:
    czg:
      run: exec < /dev/tty && pnpm czg --hook || true
      interactive: true
