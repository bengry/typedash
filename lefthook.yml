pre-commit:
  parallel: true
  commands:
    lint:
      glob: "src/**/*.{js,ts}"
      run: pnpm lint:linter:fix {staged_files}
    format:
      glob: "{*.*,**/*.*}"
      run: pnpm lint:format:fix {staged_files}
